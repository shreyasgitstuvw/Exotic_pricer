# LESSONS — corrections become rules (read at every session open)

1. (seed, from resume v3 feedback) Frame everything as research first, engineering second — the artifact is a finding, the code is its apparatus.
2. (seed, from Orion) A near-miss against a pre-registered bar is a reject, not a candidate for bar adjustment.
3. (seed, from Orion) Before re-running after a "bug fix," validate the fix against ground truth that doesn't depend on the result you want.
