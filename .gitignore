# data outputs (regenerated by the experiments/ scripts) — never commit
data/
*.parquet
*.npz
*.pt

# never commit raw market data (large / licensed)
HISTORY_NIFTY/
bhavcopy/
bhavcopy_*/
*.zip
*.csv
!configs/*.csv

# keep the money-plot images if you want them in the repo; otherwise uncomment:
# *.png

# python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
build/
dist/
!data/README.md
